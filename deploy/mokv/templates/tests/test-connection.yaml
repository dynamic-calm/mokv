apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mokv.fullname" . }}-test-connection"
  labels:
    {{- include "mokv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mokv.fullname" . }}:{{ .Values.rpcPort }}']
  restartPolicy: Never
